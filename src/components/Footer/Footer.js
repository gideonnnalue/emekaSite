import React from 'react';
import './Footer.css';

const footer = ( props ) => {
    return (
        <footer>
            <div className="container-fluid p-0 cus-footer"></div>
        </footer>
    );
}

export default footer;